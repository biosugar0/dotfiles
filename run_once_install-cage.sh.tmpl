#!/bin/bash
{{- if eq .chezmoi.os "darwin" }}
set -euo pipefail

if ! command -v cage &>/dev/null; then
  echo "==> Installing cage (with --no-quarantine)..."
  brew install --cask Warashi/tap/cage --no-quarantine
else
  echo "==> cage already installed"
fi
{{- end }}
